# -*- coding: utf-8 -*-
"""llmwiki: FTS + Vector + GraphRAG 하이브리드 검색 기반 LLM Wiki (self-evolving).

__version__ 은 릴리스 노트(docs/RELEASE_NOTES.md)의 최신 절과 같아야 하며, `python -m llmwiki --version` ·
`GET /api/status` 의 `version` · MCP `initialize` 의 `serverInfo.version` 이 모두 이 값을 쓴다.
"""
__version__ = "3.3.0"
